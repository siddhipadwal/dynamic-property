export const metadata = {
  title: 'Blog - Real Estate',
  description: 'Latest news and updates from our real estate portfolio',
};

export default function BlogLayout({ children }) {
  return children;
}
